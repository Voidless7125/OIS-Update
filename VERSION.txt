2026.08.30.2
# ---------------------------------------------------------------------
#  Line 1 above is the version string. It is parsed by Patch_OIS.bat and
#  compared against the copy on GitHub, so it must stay on its own on the
#  first line, in YYYY.MM.DD.N form, with no leading spaces.
#
#  Everything below line 1 is printed to the user when the patch runs.
#  Lines starting with # are maintainer notes and are never shown.
#
#  Keep to plain ASCII. Box-drawing characters render as garbage in the
#  console because the code page is almost never UTF-8.
#  Do not use  >  <  |  or  ^  in visible lines.
# ---------------------------------------------------------------------
===============================================================
    OBJECTS IN SPACE          C O M M U N I T Y   P A T C H
    Release 2026.08.30.2                     30 August 2026
===============================================================
    Modernises the 2018 dependency chain shipped with OIS 1.0.8.
    15 libraries, verified by SHA256, installed in one pass.
---------------------------------------------------------------
UPDATED: Edited Readme and Security.md to match new changes.
NEW: Rebuilt cURL against Windows' native TLS (Schannel) instead
     of bundled OpenSSL, removing 10 MinGW runtime files that had
     no reason to be here and were getting flagged by Windows
     Smart App Control on some systems. Payload down from 28
     files to 15. OpenAL rebuilt from the official release
     binary, fixing a debug-build mixup in the previous package.
---------------------------------------------------------------
  LIBRARY VERSIONS IN THIS RELEASE
    cURL      8.21.0 (Schannel)   SQLite     3.53.4
    OpenSSL   1.1.1zh (websockets only, see SECURITY.md)
    zlib      1.3.2        libtiff    4.7.2      vorbis  1.3.7
    mpg123    1.33.7       OpenAL     1.25.2     FMOD    2.0.1.23
    libwebsockets 2.4.2 - newest build that stays ABI compatible.
    Residual risk is contained by the optional firewall rules.
===============================================================
  Full details in SECURITY.md. Install notes in INSTALL.md.
  Report problems on the Steam discussion thread.
===============================================================